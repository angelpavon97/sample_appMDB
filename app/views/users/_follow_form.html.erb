<% unless current_user?(@user) %>
  <div id="follow_form">
  <% if current_user.following?(@user) %>
    <%= render 'follow/unfollow' %>
  <% else %>
    <%= render 'follow/follow' %>
  <% end %>
  </div>
<% end %>